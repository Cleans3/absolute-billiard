class_name ShotIntent
extends RefCounted

## The only thing the Player module hands to physics (01 §2, P1).

var aim_dir := Vector2.ZERO
var power := 0.0
var spin_offset := Vector2.ZERO
var elevation_deg := 0.0
var jump := false
var masse := false
var shooter_id := 0
